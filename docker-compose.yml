services:
  mongodb:
    image: mongo:7
    container_name: dory-mongo
    ports:
      - '27017:27017'
    volumes:
      - dory-mongo-data:/data/db
    restart: unless-stopped

volumes:
  dory-mongo-data:
